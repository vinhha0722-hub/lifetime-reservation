{
  "name": "lt-reserve",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node reserve.js",
    "test": "vitest run",
    "test:unit": "vitest run tests/utils.test.js",
    "test:mock": "playwright test tests/mock-flow.test.js",
    "test:all": "vitest run && playwright test tests/mock-flow.test.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  }
}
